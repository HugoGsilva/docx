# API Key para autenticação
# IMPORTANTE: Em produção, use uma chave gerada com:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
VITE_API_KEY=sua-chave-super-secreta-aqui

# URL da API (para produção no mesmo domínio, deixe vazio)
# VITE_API_URL=https://docx.hugogsilva.dev
